# Core dependencies
ring-doorbell==0.8.12
oauthlib==3.2.2

# AI/ML libraries
# torch and torchvision installed separately in Dockerfile (CPU version)
# ultralytics will bring in compatible numpy, pillow, opencv versions
ultralytics>=8.2.0

# Face recognition
face-recognition==1.3.0
# dlib installed separately in Dockerfile

# Configuration
pyyaml>=6.0

# Utilities
requests>=2.31.0
python-dateutil>=2.8.2
psutil>=5.9.0

# Web interface
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
sse-starlette>=1.8.2
